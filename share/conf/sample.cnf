# Sample configuration file for Web:Extend applications
# You can use, modify or redistribute it freely

# Global settings

autoload.path	= //app/php
aliases.file	=
timezone		= UTC

# Force drivers to start before running the application

start.locale	= 0
start.session	= 0

# Application settings

app.toppage = toppage

# Database settings

db.driver	= weePgSQLDatabase
db.host		= localhost
db.user		= username
db.password	= password
db.dbname	= dbname

# Locale settings

locale.driver	= weeLocale
locale.auto		= 1
locale.default	=

# Mail settings

mail.debug.to =
mail.debug.reply-to =

# Output settings

output.driver		= weeXHTMLOutput
output.cache.path	= //app/tmp/appcache
output.cache.expire	= 300
output.gzip			= 1

# Session settings

session.driver		= weeSession
session.check.ip	= 1
session.check.token	= 1

# Routing settings

routing.strict	= 0
# route.example	= toppage
